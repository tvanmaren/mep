---
description: "tidy — mid-flight branch recovery; docs only. Hands off to /prep checkpoint."
alwaysApply: false
---

# tidy

Thin handle for `.cursor/skills/tidy/SKILL.md`. Treat everything after `/tidy` as the skill
invocation (`/tidy <slug>`).

Docs and gates only. Do **not** write application code. Do **not** `git commit`.
