---
description: "execute-plan — parallel fanout of one mep brief when ownership is disjoint"
alwaysApply: false
---

# execute-plan

Thin handle for `.cursor/skills/execute-plan/SKILL.md`. Prefer this when the current brief sets
`fanout: parallel` with disjoint ownership; otherwise `/implement-plan <briefPath>`.
