---
description: "commit-prep — stage scoped paths, audit, hand off a commit message. Does not commit."
alwaysApply: false
---

# commit-prep

Thin handle for `.cursor/skills/commit-prep/SKILL.md`. Treat everything after `/commit-prep` as
the skill invocation (`/commit-prep <slug> [code|docs-bootstrap|docs-delta]`).

Do **not** `git commit`.
